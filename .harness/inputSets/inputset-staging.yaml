inputSet:
  name: staging-input
  identifier: staging_input
  pipeline:
    variables:
      environment: staging
      buildTool: npm
      dockerImage: node:18-slim
      dockerRegistry: Dockerhub
      k8sCluster: kub-desktop
      deployNamespace: staging-ns
      # secretApiKey: account.STAGING_API_KEY
