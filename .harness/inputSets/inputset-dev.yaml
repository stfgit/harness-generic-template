inputSet:
  name: dev-input
  identifier: dev_input
  pipeline:
    variables:
      environment: dev
      buildTool: npm
      dockerImage: node:20-alpine
      dockerRegistry: Dockerhub
      k8sCluster: kub-desktop
      deployNamespace: dev-ns
      # secretApiKey: account.DEV_API_KEY
