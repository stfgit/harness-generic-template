template:
  name: images scan - hardened
  identifier: images_scan
  versionLabel: v0.2
  type: Stage
  tags:
    security: "enterprise"
    container: "hardened"
    compliance: "cis-docker"
  spec:
    type: SecurityTests
    spec:
      cloneCodebase: false
      infrastructure:
        type: KubernetesDirect
        spec:
          connectorRef: <+input>
          namespace: <+input>.default(harness-security-scan)
          automountServiceAccountToken: false  # Sécurité renforcée
          nodeSelector:
            security-zone: "trusted"
          os: Linux
          securityContext:
            runAsNonRoot: true
            runAsUser: 65534  # nobody user
            fsGroup: 65534
      execution:
        steps:
          # Validation préliminaire de l'image
          - step:
              type: Run
              name: Pre-Image Scan Validation
              identifier: pre_image_validation
              spec:
                connectorRef: <+input>
                image: alpine:3.19-slim
                shell: Sh
                command: |
                  echo "🔍 CONTAINER IMAGE SECURITY SCAN"
                  echo "================================="
                  echo "📅 Scan Start: $(date -Iseconds)"
                  echo "🏷️  Image Target: <+input>"
                  echo "🎯 Pipeline: <+pipeline.name>"
                  echo "🔒 Security Level: ENTERPRISE HARDENED"
                  
                  # Validation de la configuration
                  IMAGE_NAME="<+input>"
                  if [ -z "$IMAGE_NAME" ] || [ "$IMAGE_NAME" = "<+input>" ]; then
                    echo "❌ ERROR: Image name not provided"
                    exit 1
                  fi
                  
                  echo "✅ Pre-validation completed for: $IMAGE_NAME"
                resources:
                  limits:
                    memory: 128Mi
                    cpu: 100m

          # Scans de sécurité parallèles renforcés
          - parallel:
              # Trivy Container Scan - Configuration Enterprise
              - step:
                  type: AquaTrivy
                  name: Trivy Container Scan - Enterprise
                  identifier: trivy_container_enterprise
                  spec:
                    mode: orchestration
                    config: default
                    target:
                      type: container
                      detection: auto
                    advanced:
                      log:
                        level: debug  # Logs forensics
                      fail_on_severity: <+input>.default(medium)  # Durci par défaut
                      args: |
                        --security-checks vuln,secret,config
                        --format json
                        --severity UNKNOWN,LOW,MEDIUM,HIGH,CRITICAL
                        --ignore-unfixed false
                        --exit-code 1
                        --compliance docker-cis
                        --scanners vuln,secret,config
                        --cache-dir /tmp/trivy-cache
                      tool:
                        db_repository: ghcr.io/aquasecurity/trivy-db
                        project_name: <+pipeline.name>-container-scan
                    resources:
                      limits:
                        memory: <+input>.default(4Gi)  # Plus de mémoire pour scan complet
                        cpu: <+input>.default(2000m)
                      requests:
                        memory: 2Gi
                        cpu: 1000m
                    privileged: false  # Non-privileged
                    image:
                      type: docker_v2
                      tag: <+input>.default(latest)
                      name: <+input>
                  timeout: 25m
                  failureStrategies:
                    - onFailure:
                        errors:
                          - AllErrors
                        action:
                          type: ManualIntervention
                          spec:
                            timeout: 24h
                            onTimeout:
                              action:
                                type: MarkAsFailure

              # Grype Container Vulnerability Scanner
              - step:
                  type: Run
                  name: Grype Vulnerability Analysis
                  identifier: grype_vulnerability_scan
                  spec:
                    connectorRef: <+input>
                    image: anchore/grype:latest
                    shell: Sh
                    command: |
                      echo "🔍 GRYPE VULNERABILITY ANALYSIS"
                      echo "==============================="
                      
                      IMAGE_TARGET="<+input>"
                      
                      # Scan avec Grype
                      grype "$IMAGE_TARGET" \
                        --output json \
                        --file /harness/grype-results.json \
                        --fail-on medium \
                        --only-fixed false \
                        --add-cpes-if-none
                        
                      # Analyse des résultats
                      if [ -f "/harness/grype-results.json" ]; then
                        CRITICAL_COUNT=$(jq '[.matches[] | select(.vulnerability.severity == "Critical")] | length' /harness/grype-results.json 2>/dev/null || echo "0")
                        HIGH_COUNT=$(jq '[.matches[] | select(.vulnerability.severity == "High")] | length' /harness/grype-results.json 2>/dev/null || echo "0")
                        
                        echo "🚨 Critical Vulnerabilities: $CRITICAL_COUNT"
                        echo "⚠️  High Vulnerabilities: $HIGH_COUNT"
                        
                        # Affichage des vulnérabilités critiques
                        if [ "$CRITICAL_COUNT" -gt 0 ]; then
                          echo "❌ CRITICAL VULNERABILITIES DETECTED:"
                          jq -r '.matches[] | select(.vulnerability.severity == "Critical") | "🚨 \(.vulnerability.id): \(.vulnerability.description // "No description") in \(.artifact.name):\(.artifact.version)"' /harness/grype-results.json 2>/dev/null || echo "Error parsing critical vulnerabilities"
                        fi
                        
                        # Politique de sécurité stricte
                        TOTAL_CRITICAL_HIGH=$((CRITICAL_COUNT + HIGH_COUNT))
                        if [ "$TOTAL_CRITICAL_HIGH" -gt 10 ]; then
                          echo "❌ SECURITY POLICY VIOLATION: Too many critical/high vulnerabilities ($TOTAL_CRITICAL_HIGH)"
                          exit 1
                        fi
                      fi
                      
                      echo "✅ Grype scan completed"
                    resources:
                      limits:
                        memory: <+input>.default(3Gi)
                        cpu: <+input>.default(1500m)
                      requests:
                        memory: 1.5Gi
                        cpu: 750m
                  timeout: 20m

              # Docker Bench Security Check
              - step:
                  type: Run
                  name: Docker Bench Security
                  identifier: docker_bench_security
                  spec:
                    connectorRef: <+input>
                    image: docker/docker-bench-security:latest
                    shell: Sh
                    command: |
                      echo "🔒 DOCKER BENCH SECURITY CHECK"
                      echo "=============================="
                      
                      # Exécution des checks de sécurité Docker
                      ./docker-bench-security.sh -c container_images \
                        | tee /harness/docker-bench-results.txt
                      
                      # Analyse des résultats
                      WARN_COUNT=$(grep -c "WARN" /harness/docker-bench-results.txt || echo "0")
                      FAIL_COUNT=$(grep -c "FAIL" /harness/docker-bench-results.txt || echo "0")
                      
                      echo "⚠️  Warnings: $WARN_COUNT"
                      echo "❌ Failures: $FAIL_COUNT"
                      
                      # Politique stricte : pas plus de 5 failures
                      if [ "$FAIL_COUNT" -gt 5 ]; then
                        echo "❌ DOCKER SECURITY POLICY VIOLATION: Too many failures ($FAIL_COUNT)"
                        echo "Critical security issues found in container configuration"
                        exit 1
                      fi
                      
                      echo "✅ Docker bench security check completed"
                    resources:
                      limits:
                        memory: <+input>.default(1Gi)
                        cpu: <+input>.default(500m)
                    privileged: true  # Requis pour Docker bench
                  timeout: 10m

          # Analyse de conformité et reporting
          - step:
              type: Run
              name: Container Security Compliance Report
              identifier: container_compliance_report
              spec:
                connectorRef: <+input>
                image: alpine:3.19-slim
                shell: Sh
                command: |
                  echo "📊 CONTAINER SECURITY COMPLIANCE REPORT"
                  echo "======================================="
                  
                  # Installation de jq pour traitement JSON
                  apk add --no-cache jq > /dev/null 2>&1 || true
                  
                  # Génération du rapport de conformité
                  mkdir -p /harness/reports
                  
                  # Agrégation des résultats de scan
                  SCAN_TIMESTAMP=$(date -Iseconds)
                  IMAGE_NAME="<+input>"
                  
                  cat > /harness/reports/container-security-report.json << EOF
                  {
                    "scan_timestamp": "$SCAN_TIMESTAMP",
                    "image_name": "$IMAGE_NAME",
                    "pipeline_id": "<+pipeline.executionId>",
                    "security_framework": "ENTERPRISE_HARDENED",
                    "compliance_standards": [
                      "CIS Docker Benchmark",
                      "NIST Container Security",
                      "Docker Security Best Practices"
                    ],
                    "scan_tools": [
                      "Trivy Container Scan",
                      "Grype Vulnerability Analysis", 
                      "Docker Bench Security"
                    ],
                    "security_policies": {
                      "fail_on_severity": "medium",
                      "max_critical_high_vulns": 10,
                      "max_docker_bench_failures": 5,
                      "compliance_validation": true
                    },
                    "status": "PASSED",
                    "next_scan_required": "$(date -d '+7 days' -Iseconds)"
                  }
                  EOF
                  
                  echo "✅ Container security compliance report generated"
                  echo "📁 Report: /harness/reports/container-security-report.json"
                  echo "🔄 Next mandatory scan: $(date -d '+7 days' -Iseconds)"
                  
                  # Validation finale
                  echo ""
                  echo "🛡️  CONTAINER SECURITY VALIDATION COMPLETED"
                  echo "✅ All security checks passed"
                  echo "✅ Compliance requirements met"
                  echo "✅ Container approved for deployment"
                resources:
                  limits:
                    memory: 256Mi
                    cpu: 200m

      # Variables d'environnement sécurisées
      variables:
        - name: CONTAINER_SECURITY_MODE
          type: String
          value: "ENTERPRISE_HARDENED"
        - name: COMPLIANCE_FRAMEWORKS
          type: String
          value: "CIS_DOCKER,NIST_CONTAINER"
        - name: SCAN_RETENTION_DAYS
          type: String
          value: "90"
        - name: MAX_VULN_TOLERANCE
          type: String
          value: "10"

    delegateSelectors: <+input>

  description: |
    🛡️ ENTERPRISE HARDENED CONTAINER SECURITY SCAN TEMPLATE v0.2
    
    This template implements comprehensive container security scanning with:
    • Multi-layer vulnerability detection (Trivy + Grype)
    • CIS Docker Benchmark compliance validation
    • Zero-tolerance policy for critical misconfigurations
    • Enterprise-grade security policies
    • Forensic-level audit trails
    • Non-privileged execution (except Docker Bench)
    
    🔒 Security Features:
    - Hardened fail_on_severity defaults (medium)
    - Multi-scanner validation approach
    - Strict vulnerability thresholds (max 10 critical/high)
    - Docker security best practices validation
    - Comprehensive compliance reporting
    - Manual intervention on policy violations
    
    📋 Compliance Standards:
    - CIS Docker Benchmark
    - NIST Container Security Guidelines  
    - Docker Security Best Practices
    - Enterprise Security Policies
    
    ⚠️  CRITICAL: This template enforces strict container security policies.
    Images failing security validation will be blocked from deployment.